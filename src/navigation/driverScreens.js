export const {
  default: DriverWelcome,
} = require('../screens/driver/DriverWelcome');

export const {
  default: DriverPhone,
} = require('../screens/driver/driverAuth/DriverPhone');

export const {
  default: DriverOtp,
} = require('../screens/driver/driverAuth/DriverOtp');

export const {
  default: DriverPassword,
} = require('../screens/driver/driverAuth/DriverPassword');

export const {
  default: DriverDetails,
} = require('../screens/driver/driverAuth/DriverDetails');

export const {
  default: TermCondition,
} = require('../screens/driver/driverAuth/TermCondition');

export const {default: SetAll} = require('../screens/driver/driverAuth/SetAll');

export const {
  default: RaidoChoose,
} = require('../screens/driver/driverVarification/RaidoChoose');

export const {
  default: RequireSteps,
} = require('../screens/driver/driverVarification/RequireSteps');

export const {
  default: BackCnic,
} = require('../screens/driver/driverVarification/BackCnic');

export const {
  default: FrontCnic,
} = require('../screens/driver/driverVarification/FrontCnic');

export const {
  default: DriverPhoto,
} = require('../screens/driver/driverVarification/DriverPhoto');

export const {
  default: LicenseFrontSide,
} = require('../screens/driver/driverVarification/LicenseFrontSide');

export const {
  default: VechicleDocuments,
} = require('../screens/driver/driverVarification/VechicleDocuments');

export const {
  default: DriverApproval,
} = require('../screens/driver/driverVarification/DriverApproval');

export const {
  default: LicenseBackSide,
} = require('../screens/driver/driverVarification/LicenseBackSide');
