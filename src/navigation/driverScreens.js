export const {
  default: DriverWelcome,
} = require('../screens/driver/DriverWelcome');

export const {
  default: DriverPhone,
} = require('../screens/driver/driverAuth/DriverPhone');

export const {
  default: DriverOtp,
} = require('../screens/driver/driverAuth/DriverOtp');

export const {
  default: DriverPassword,
} = require('../screens/driver/driverAuth/DriverPassword');

export const {
  default: DriverDetails,
} = require('../screens/driver/driverAuth/DriverDetails');

export const {
  default: TermCondition,
} = require('../screens/driver/driverAuth/TermCondition');
